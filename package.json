{
  "name": "peerverse-fullstack",
  "version": "1.0.0",
  "scripts": {
    "build": "cd FRONTEND/frontone && npm install && npm run build",
    "start": "cd BACKEND && npm start"
  },
  "engines": {
    "node": "22.x"
  }
}