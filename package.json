{
  "name": "peerverse",
  "version": "1.0.0",
  "scripts": {
    "build": "cd FRONTEND/frontone && npm install && npm run build && cp -r build/* ../../",
    "start": "serve -s ."
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}