{
  "version": 2,
  "buildCommand": "cd FRONTEND/frontone && npm install && npm run build",
  "outputDirectory": "FRONTEND/frontone/build",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}